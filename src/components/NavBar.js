import React from "react";

function NavBar() {
  // update the JSX being returned!
  return <nav>NavBar
    <a href="#home">Home</a>
    <a href="#about">About</a>        
  </nav>;
}

export default NavBar;
